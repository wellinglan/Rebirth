# Plan Hierarchy & Date UX Manual Test

1. 启动 App。
2. 进入 Plan。
3. 点击“新建目标”。
4. 确认开始日期默认为今天。
5. 确认默认目标层级为“月度”。
6. 确认目标日期自动为开始日期加一个自然月。
7. 将开始日期改为 `2026-03-31`，确认月度目标日期为 `2026-04-30`。
8. 切换目标层级为“季度”，确认目标日期自动调整为开始日期加三个自然月。
9. 切换目标层级为“自定义”，确认目标日期变为可编辑。
10. 使用年、月、日下拉组件选择日期。
11. 分别双击年、月、日并手动填写数值。
12. 将“优先级”填为 `-1`，确认显示校验错误且不能保存。
13. 创建一个根目标。
14. 点击根目标的“添加子目标”。
15. 填写并创建子目标。
16. 点击根目标的“子目标”进入 child view。
17. 确认刚创建的子目标出现在列表中。
18. 点击返回上一级，确认回到根目标列表。
19. 关闭并重新启动 App。
20. 确认根目标和子目标仍然存在。
21. 确认 Today 和 Journal 页面仍可正常打开。

## Android 竖屏布局检查

1. 在 Android 真机竖屏下打开“新建目标”或“编辑目标”。
2. 分别检查“开始日期”和“目标日期”的年、月、日输入控件。
3. 确认四位年份保持单行显示，月份和日期不会被压缩、截断或逐字换行。
4. 确认日期清除按钮不会挤压年、月、日输入控件。
5. 确认两个日期区块之间留有清晰间距，表单可正常滚动且没有横向溢出。

## 执行记录（2026-07-20）

| 平台 | 结果 | 已通过 | 失败项 | 证据 |
|---|---|---|---|---|
| Windows | PASS | 本轮其余人工测试均通过。 | 无已报告失败。 | 用户人工测试反馈。 |
| Android 真机竖屏 | FAIL | 本轮其余人工测试均通过，Plan 数据编辑与保存功能可用。 | “开始日期”和“目标日期”的年、月、日控件横向空间不足；四位年份被逐字换行，月份、日期与清除按钮排列拥挤。 | 用户提供的 Android 真机截图 `000cf7f70d8b2c64157cda60bbcd4b6a.jpg`。 |

### 缺陷记录

- 缺陷 ID：`PLAN-ANDROID-DATE-LAYOUT-001`
- 严重程度：中等。功能仍可操作，但日期可读性和输入体验明显受损。
- 复现条件：Android 真机竖屏，打开 Plan 的目标新建或编辑 Dialog。
- 实际结果：年、月、日与清除按钮被固定在同一横向区域；年份输入框过窄，`2026` 显示为纵向字符。
- 期望结果：窄屏下日期控件应改为可读的响应式布局，四位年份保持单行，所有输入和操作按钮均不重叠、不截断。
- 当前结论：Plan Android 日期 UX 不通过；修复并完成 Android 真机回归前，不应将该检查标记为 PASS。

## Sprint 8F.1 修复验证（2026-07-20）

- 修复源码提交：`f7989ce6d6d44723ace7fa172a0b831df0fca1b5`
- 响应式规则：日期字段可用宽度小于 `440px`，或文字缩放大于 `1.3` 时使用堆叠布局。
- 窄屏布局：年份独占一行，月份和日期在下一行；清除按钮位于日期标题行，不参与输入字段宽度分配。
- 宽屏布局：年份、月份、日期保持同一行，年份使用更高 flex，清除按钮仍独立于输入字段。
- 自动测试：`320`、`360`、`412`、`720`、`1200px` 与 `1.0`、`1.3`、`1.5`、`2.0` 文字缩放均通过，无日期组件 RenderFlex overflow。
- 交互测试：单击选择、双击手动输入、Tab、Enter、Space 与日期 Semantics 均通过。
- 语义回归：月度、季度、自定义日期和开始日期变化后的目标日期计算测试均通过；日期计算代码未修改。
- 发布构建：Windows release 与 Android split-per-ABI release 均构建成功。
- 真机状态：`AWAITING RETEST`。必须安装新的 `app-arm64-v8a-release.apk` 后重新执行本文件的 Android 竖屏布局检查。
- 缺陷状态：`FIXED IN BUILD / NOT YET VERIFIED ON DEVICE`。真机复测通过前，不关闭 `PLAN-ANDROID-DATE-LAYOUT-001`。

## Sprint 8F.1 Android 真机复测（2026-07-20）

- 结果：PASS（用户确认）。
- 安装产物：Sprint 8F.1 重建的 `app-arm64-v8a-release.apk`。
- Plan 新建/编辑：PASS。
- 开始日期与目标日期：PASS。
- 四位年份水平完整显示：PASS。
- 月份、日期与清除按钮：PASS，无宽度争抢。
- 大字号与页面滚动：PASS。
- 保存目标与重启持久化：PASS。
- 缺陷状态：`PLAN-ANDROID-DATE-LAYOUT-001` 已通过 Android 真机回归，可以关闭。
